name: sendMessageToDiscord
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: curl -H "Content-Type: application/json" -d "{\"content\": \"${{ vars.MY_MESSAGE }}\"}" ${{ secrets.DISCORD_WEBHOOK }}
